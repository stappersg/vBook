# vsmtp-plugin-awesome

> Add description here.

## Setup

Set the same hashing key in `.cargo/config.toml` than the one in the Rust code that will import this dynamic library.

## Build

```rust
$ cargo build
```